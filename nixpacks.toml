[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.build]
cmds = [
  "npm install",
  "npm run build"
]

[phases.start]
cmds = ["npm install --production=false", "npm run preview"]

[start]
cmd = "npm run preview"

[variables]
NODE_ENV = "production"
PORT = "3000"