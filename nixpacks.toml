[phases.setup]
nixPkgs = ["nodejs_20", "nodePackages.npm"]

[phases.build]
cmds = [
  "npm install",
  "npm run build"
]

[phases.start]
cmds = ["npm run preview"]

[start]
cmd = "npm run preview"

[variables]
NODE_ENV = "production"
PORT = "3000"